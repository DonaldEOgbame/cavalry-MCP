# Knowledge source staging

Place explicitly licensed or locally permitted source material here before ingestion. Keep source classes in separate directories. Source files are treated as untrusted reference data and are never executed.

Official documentation is global by default. User scenes, scripts, components, failures, and visual results should be project- or session-scoped unless a user explicitly opts into reusable global knowledge.
